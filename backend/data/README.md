# data/

This folder is mounted into the Docker containers at `/data`.

- `/data/gdrive_mirror` is the recommended local mirror of Google Drive for RAG ingest.
- Do not commit any private documents or API keys into git.
